SELECT M.title
FROM Movies M, Tags T, Tag_Names K
WHERE (M.title IN (SELECT M.title
					FROM Movies M, Tags T, Tag_Names K
					WHERE M.mid=T.mid and T.tid=K.tid and K.tag LIKE '%bad%') and M.mid=T.mid and T.tid=K.tid and K.tag LIKE '%good%')
